rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permitir lectura y escritura a usuarios autenticados
      // En producción se recomienda segmentar por tenantId en la ruta
      allow read, write: if request.auth != null;
    }
  }
}